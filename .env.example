# Example environment values for local development
# Copy to `.env` and adjust per deployment target

NODE_ENV=development
NETLIFY_SITE_ID=replace-with-your-site-id
# Optional: supply mocked secrets for other functions
OPENAI_API_KEY=sk-...-example
